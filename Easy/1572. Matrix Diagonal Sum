class Solution {
    public int diagonalSum(int[][] mat) {
         int sum=0;
         int col=mat.length-1;
         for(int i=0;i<mat.length;i++)
         {
            sum+=mat[i][i];
            if(i!=col-i)
            sum+=mat[i][col-i];
         }
         return sum;
    }
}
